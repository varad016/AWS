why??

to connect multiple services
API Gateway takes an HTTP request and converts it into a Python dictionary (event) for Lambda
HTTP(website)-> req -> py directory

GET vs POST difference

GET → queryStringParameters
POST → event["body"] (string → JSON)